services:
  - type: web
    name: guimi-xiaozhen
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    disk:
      name: data-disk
      mountPath: /data
      sizeGB: 1
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATA_DIR
        value: /data
